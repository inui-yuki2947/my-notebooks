# Ruby/モジュール化/インポート

## require

### 記法

| 記法                   | 概要                               |
| ---------------------- | ---------------------------------- |
| `require ファイルパス` | 指定したファイルをインポートする。 |

- ファイルパスでは、`./`は必須で、拡張子は任意。
